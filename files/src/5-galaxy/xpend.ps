/in. {72 mul} def
/line 1024 string def
/leftmargin where not{/leftmargin .75 in. def}{pop}ifelse
/printfile
{	leftmargin 10.5 in. moveto
	{currentfile cvlit line readline not{exit}if
	 (\f) search{show showpage pop}if
	 show
 	 leftmargin currentpoint exch 
	 pop 13 sub dup .5 in. le{pop pop showpage}{moveto}ifelse
	}loop
	showpage
}bind def
/Times-Roman findfont 11 scalefont setfont
printfile
