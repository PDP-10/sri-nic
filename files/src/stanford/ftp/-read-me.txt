This directory, PS:<FTP.EXPORT> on SU-Sierra, contains the files necessary
to build a TCP only version of the Stanford user FTP.

The directory PS:<FTP> contains the sources to build a PUP and TCP version
of Stanford FTP.  It may have more recent sources.

Problems/questions/bugs should be directed to BUG-FTP@SU-SIERRA.ARPA.

