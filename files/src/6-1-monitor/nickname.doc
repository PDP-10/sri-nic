GTDOM% procedure:

take string from user.  if it starts with "[" then try to parse
an internet address and return the number.  otherwise if string 
had no dots, replace with entry from nickname table (if any),
then try to resolve name.  if resolution fails, look up in host
table.  if not found return temporary error.
